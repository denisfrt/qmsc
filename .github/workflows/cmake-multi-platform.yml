# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Release]
        include:
          - os: windows-latest
            cmake_defs: '-DWIN32=ON'
            artifact: 'qmsc_window.tar.gz'
          - os: ubuntu-latest
            cmake_defs: ''
            artifact: '/qmsc.AppImage'

    steps:
    - name: Install Qt (Linux)
      uses: jurplel/install-qt-action@v4
      if: matrix.os == 'ubuntu-latest'
      with:
        version: '6.10.1'
        host: 'linux'
        target: 'desktop'
        arch: 'linux_gcc_64'

    - name: Install Qt (Windows)
      uses: jurplel/install-qt-action@v4
      if: matrix.os == 'windows-latest'
      with:
        version: '6.10.1'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_mingw'

    - name: Checkout
      uses: actions/checkout@v6
      with:
        lfs: true

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"
        echo "install-output-dir=${{ github.workspace }}/dist" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -Wno-dev -G "Ninja Multi-Config"
        -B ${{ steps.strings.outputs.build-output-dir }}
        -S ${{ github.workspace }}
        ${{ matrix.cmake_defs }}

    - name: Build
      run: >
        cmake --build ${{ steps.strings.outputs.build-output-dir }}
        --config ${{ matrix.build_type }}

    - name: Install
      run: >
        cmake --install ${{ steps.strings.outputs.build-output-dir }}
        --config ${{ matrix.build_type }}
        --prefix ${{ steps.strings.outputs.install-output-dir }}

    - name: Package (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        wget -nv -c https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
        ARCH=x86_64 ./appimagetool-x86_64.Appimage ${{ steps.strings.outputs.install-output-dir }}/AppDir ${{ steps.strings.outputs.install-output-dir }}/qmsc.AppImage

    - name: Upload Artifact (Linux)
      if: matrix.os == 'ubuntu-latest'
      uses: actions/upload-artifact@v7
      with:
        path: ${{ steps.strings.outputs.install-output-dir }}/${{ matrix.artifact }}
        archive: false

    - name: Upload Artifact (Windows)
      if: matrix.os == 'windows-latest'
      uses: actions/upload-artifact@v7
      with:
        name: ${{ matrix.artifact }}
        path: ${{ steps.strings.outputs.install-output-dir }}/AppDir
